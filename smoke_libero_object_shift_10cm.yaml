# Discovery: 10 cm +y displacement of the pick target on Spatial task 0.
server:
  url: "ws://host.docker.internal:8000"
  timeout: 180

docker:
  image: ghcr.io/allenai/vla-evaluation-harness/libero:latest
  volumes:
    - "/home/lychee/research/vla-evaluation-harness/src/vla_eval/benchmarks/libero/object_shift.py:/workspace/src/vla_eval/benchmarks/libero/object_shift.py:ro"

output_dir: "./results/smoke_libero_object_shift_10cm"

benchmarks:
  - benchmark: "vla_eval.benchmarks.libero.object_shift:LIBEROObjectShiftBenchmark"
    name: "pi05_libero_spatial_objshift_10cm_smoke"
    episodes_per_task: 1
    max_tasks: 1
    params:
      suite: libero_spatial
      seed: 7
      num_steps_wait: 10
      object_shift_cm: 10
      object_shift_axis: y
      send_state: true
      send_wrist_image: true
    recording:
      record_video: true
      video_fps: 10
